# MALLO 브리핑 — 모바일 앱 화면 디자인·플로우 확인 작업 (1단계 · iOS 단일 뷰포트)

> 새 Claude Code 세션 시작 시 이 파일을 먼저 읽게 하고 작업을 시작한다.
> InnerDerma 프론트엔드 레포(`innerderma_front`)에서 검증된 작업 방식을 **MALLO** 프로젝트용으로 이식·재구성한 문서.
> 문서의 "오늘"은 이 1단계 작업에 실제로 착수하는 날을 가리킨다 — 문서 작성일과 착수일이 다르면 아래 기술 스택 버전(2번)을 그 시점 기준으로 다시 확인할 것.

구성 한 줄 요약: **React 19 + TypeScript / Vite 8 빌드 / Zustand 상태 / Tailwind CSS 4 스타일 / Axios 통신.** Figma 프레임을 단일 뷰포트(iOS 393×852) 기준으로 퍼블리싱하고, Playwright 픽셀 대조 + 기능 자동 검증으로 화면·플로우를 확인한다. (오늘은 화면 디자인/플로우 확인이 목적이라 iOS/Android 두 뷰포트를 각각 만들지 않는다 — 아래 3번 참고)

## 0. 채워야 할 빈칸 · 전제조건 (새 세션 시작 전 확인)

- **프로젝트 경로**: `______________________` (MALLO 레포 경로 지정)
- **Figma 디자인 소스**: `______________________` (구현할 화면들의 node-id 링크)
- **오늘 마감**: 화면 캡처 제출 완료
- **Figma MCP 연결 확인** — 6번의 Figma 규칙(`get_metadata`/`get_design_context`)은 Figma MCP 플러그인이 새 세션 환경에 연결돼 있어야 동작한다. 연결 안 돼 있으면 새 세션 시작 전에 먼저 설정할 것.
- **Node 버전 확인** — Vite 8은 Node 18을 지원하지 않는다. 새 세션 시작 전 `node -v`로 20 이상인지 확인하고, 아니면 먼저 업그레이드한다.

---

## 1. 지금 이 작업의 목표와 범위

- **최종 목표**: iOS/Android 둘 다 지원하는 네이티브(또는 크로스플랫폼) 모바일 앱.
- **오늘의 목표(1단계)**: 실제 네이티브 앱이 아니라, **화면 디자인·플로우를 확인할 수 있는 산출물**을 오늘 안에 만들어 캡처해서 제출한다.
- **1단계 이후(2단계, 별도 작업)**: 오늘 만든 화면/플로우를 기반으로 실제 동작하는 네이티브 앱으로 옮겨간다. 오늘 작업은 "버릴 프로토타입"이 아니라 "다음 단계의 참고 자료"이므로, 화면 구성·플로우·상태 흐름은 명확하게 남겨야 하지만 네이티브 코드로 그대로 재사용될 것은 아니라는 점을 감안해 과하게 정교화하지 않는다.

---

## 2. 기술 스택 (TypeScript 기준, 2026년 9월 조사 시점 최신 안정 버전)

> 새 프로젝트 시작 시점에 `npm create vite@latest`로 실제 최신 버전을 한 번 더 확인 권장 — 버전은 빠르게 바뀐다.

| 패키지 | 버전 | 역할 · 비고 |
|---|---|---|
| React | 19.2.x | UI 라이브러리. Actions/`use` API 안정화된 라인 |
| TypeScript | 7.0.x | 컴파일러가 Go 기반으로 전환되어 이전 라인 대비 대폭 빨라짐 |
| Vite | 8.0.x | 빌드/개발 서버. 내부적으로 Rolldown 번들러 사용, **Node 18 지원 종료** — Node 20+ 확인 |
| Tailwind CSS | 4.3.x | 유틸리티 CSS. **v3와 달리 JS config가 아니라 CSS 기반 설정**(`@theme` 등) — InnerDerma의 `tailwind.config.js` 패턴을 그대로 못 씀, 새로 익혀야 함 |
| Zustand | 5.0.x | 전역 상태. API 큰 변화 없음, InnerDerma 때와 동일하게 사용 가능 |
| React Router | `react-router` 8.x | **주의**: v8부터 `react-router-dom` 패키지가 폐지되고 `react-router` 하나로 통합(ESM-only). `import ... from 'react-router-dom'`을 그대로 복사하면 안 됨 |
| Axios (또는 동급 HTTP 클라이언트) | 최신 안정 버전 확인 | InnerDerma와 동일하게 인터셉터가 토큰 첨부·401 처리를 전담하고, 컴포넌트는 도메인 모듈(`src/api/<도메인>.ts`)만 호출 |
| Playwright | 1.62.x | 캡처/검증용 dev 의존성 |

React+Vite+TS 조합 자체는 2026년에도 Figma 기반 프로토타이핑에 여전히 표준적인 선택으로 확인됨(다른 프레임워크로 바꿀 이유 없음).

---

## 3. 1단계 구현 방식 — 웹 기반 모바일 프레임 캡처

네이티브 빌드 없이 오늘 안에 "모바일 앱처럼 보이는" 화면을 뽑기 위해, InnerDerma 프론트엔드에서 썼던 방식을 그대로 이식한다.

- **모바일 프레임 컴포넌트**: 화면을 실제 기기 크기 그대로 그리는 `DeviceFrame` 같은 래퍼를 만들고, 브라우저 창이 작아도 `transform: scale`로만 축소한다. **내부 좌표 자체를 줄이면 다 어긋난다** — 이건 InnerDerma에서 실제로 겪은 실수다.
- **뷰포트는 하나만** — 오늘은 화면 디자인·플로우 확인이 목적이라 iOS/Android 두 크기를 각각 만들지 않는다. **iOS 기준 393×852(iPhone 16) 하나로만** 작업한다. iOS/Android 둘 다 지원한다는 최종 목표(위 1번)는 2단계 네이티브 전환 때 각 플랫폼 빌드로 자연스럽게 해결되는 부분이라, 오늘 웹 프로토타입 단계에서 두 뷰포트를 미리 만들 필요가 없다.
- **캡처 스크립트**: Playwright로 각 화면을 이 단일 뷰포트에서 스크린샷 떠서 저장하는 스크립트 하나 작성(InnerDerma의 `scripts/shoot.mjs` 참고). 화면이 늘어나도 스크립트 하나로 전체를 다시 찍을 수 있게.
- **더미 데이터로 플로우만 연결**: 백엔드 연동 없이 상수/더미 데이터로 화면 간 이동(라우팅)과 주요 상태 전환(선택/토글/모달 등)만 눈으로 확인 가능하게 만든다. API 연동은 2단계 이후.

---

## 4. 아키텍처 규칙 (InnerDerma에서 굳힌 것 — 스택이 바뀌어도 그대로 이식)

- **디자인 토큰 단일 소스** — Tailwind v4는 JS config가 아니라 CSS를 직접 읽으므로, **CSS `@theme` 블록 하나가 유일한 소스**다. `theme.ts` 같은 JS 파일을 따로 만들어 색을 중복 정의하지 않는다. 코드에서 색을 JS 값으로 써야 하는 경우(예: Chart.js 팔레트)가 생기면 값을 복제하지 말고 `getComputedStyle`로 CSS 변수를 읽어오거나 CSS 변수를 그대로 참조한다. 컴포넌트에서 색상 리터럴(`#fff`, `text-[#...]`) 직접 사용 금지. Figma에서 뽑은 값은 전부 `@theme`에 이름 붙여 등록.
- **API는 도메인 모듈 경유만** — 화면 컴포넌트에서 HTTP 클라이언트를 직접 부르지 않고 `src/api/<도메인>.ts`를 거친다. 인증 토큰은 인터셉터가 전담해 화면 코드가 깨끗해진다. (오늘 1단계는 더미 데이터라 실제 호출은 없지만, 2단계 대비해 API 모듈 자리만 미리 비워두면 좋다.)
- **오버레이는 라우트가 아니다** — 모달·바텀시트처럼 "화면 위에 겹치는" UI는 URL을 바꾸는 라우트로 만들지 않고 전역 상태(예: `uiStore`)로 열고 닫는다. 한 곳(`AppModals`류 컴포넌트)에서 렌더. URL이 안 바뀌니 뒤로가기 동작이 자연스럽다.
- **모바일 프레임은 스케일로만 축소** — 위 3번과 동일한 원칙. 고정 프레임 크기를 유지하고 `transform: scale`로만 줄인다.
- **고정 레이어와 스크롤 영역 분리** — 헤더/탭바처럼 항상 보이는 레이어는 스크롤 밖에 고정하고, 콘텐츠 영역만 스크롤한다.
- **날짜/시간은 항상 실계산, 하드코딩 금지** — 디자인 목업의 특정 날짜를 상수로 박지 않고 실제 날짜 기준으로 계산한다. 검증 스크립트의 기대값도 실행 시점 날짜에서 계산해야 한다(테스트가 특정 날짜에서만 통과하는 걸 방지).
- **정적 디자인과 실제 동작이 어긋나면 동작을 택한다** — Figma는 스냅샷 한 장이라 상태(빈 값/부분 선택/비활성 등)를 표현하지 못한다. "고를 게 없으면 저장 버튼 비활성" 같은 실사용 논리가 정적 목업과 다르면 논리를 우선한다.
- **디자인 출력은 그대로 믿지 않는다** — 숨겨진 레이어, 자식 없는 빈 프레임, 겹쳐진 레이어 중 위에 있는 것만 보이는 경우 등으로 코드 출력과 실제 렌더가 다를 수 있다. 스크린샷·픽셀 실측으로 재확인 후 반영.

---

## 5. 검증 방식 — 타입 체크만으로는 부족한 부분 메우기

TypeScript를 쓰더라도 타입 체크는 "값이 어긋난다"까지는 못 잡는다(레이아웃 좌표, 색상, 텍스트 오차 등). InnerDerma에서는 두 축으로 검증했고, 오늘처럼 캡처가 최종 산출물인 작업일수록 이 방식이 그대로 유효하다.

| 축 | 방법 |
|---|---|
| 픽셀 대조 | 구현 화면을 캡처해 Figma 원본과 좌(원본)/중(구현)/우(차이) 3분할로 비교, 불일치율·문제 구간을 수치로 출력 |
| 기능 자동 검증 | Playwright로 스크롤 고정, 모달 여닫힘, 폼 입력, 상태 동기화, 날짜 이동, 필터/토글 등 시나리오를 자동 클릭·검증. 날짜 하드코딩 금지 원칙과 동일하게 기대값도 실행 시점에 계산 |

검증 스크립트를 돌릴 때 자주 놓치는 함정 두 가지도 그대로 옮겨둔다.
- **로케일 고정** — 다국어를 붙인 앱은 브라우저 로케일로 언어를 추론하므로, 테스트 러너 기본 로케일(예: en-US)이 실제 타깃 로케일(ko-KR)과 다르면 요소 탐색이 통째로 깨진다.
- **자체 서명 HTTPS 허용** — 카메라 등 `getUserMedia`류 기능은 보안 컨텍스트에서만 동작하는데, `localhost`가 아닌 LAN IP로 다른 기기에서 접속하려면 HTTPS가 필요하다. 로컬 자체 서명 인증서를 쓰는 검증 환경에서는 인증서 오류 무시 옵션을 켜야 한다. (이 프로젝트에 카메라 등 기기 API가 들어간다면 특히 해당.)

**멘토링 포인트**: 정적 타입 체크만으로 못 잡는 영역(레이아웃·색상·시각적 어긋남)이 있다면, 그 자리를 메울 검증 게이트(픽셀 대조 + 시나리오 자동화)를 처음부터 세트로 설계해야 한다.

---

## 6. Claude Code(AI 코딩 에이전트) 운영 규칙 — CLAUDE.md에 코드화할 것

말로 매번 설명하는 대신 새 저장소 루트 `CLAUDE.md`(또는 `AGENTS.md`)에 "절대 규칙"으로 박아둔다. InnerDerma에서 실제로 쓴 6개 규칙을 그대로 옮긴다 — 새 세션이 이 문서를 읽는 즉시 지켜야 할 규칙들이다.

1. **배포/인프라 경계 명시** — 이 프로젝트가 어디까지 책임지는지(예: 화면/프론트 코드만인지, 스토어 배포 파이프라인까지인지) 문서에 명시한다. 서버·인증서·다른 서비스처럼 경계 밖 변경이 필요해 보이면 **실행 전 사용자에게 먼저 확인**한다. 이렇게 문서화해두면 AI가 스코프를 넘어가는 걸 막을 수 있다.
2. **커밋/푸시는 검증 후 + 사용자 확인 후** — 테스트 통과·빌드 성공·기능 동작 확인 등 **검증이 끝난 뒤에만** `git commit`/`git push`를 진행하되, 실행 전 반드시 사용자 확인을 받는다. 자동 커밋·푸시로 바로 이어가지 않는 게 기본값이고, 예외는 세션에서 사용자가 명시할 때만.
3. **응답 언어 고정** — 모든 응답은 한글로. 사용자가 다른 언어를 명시하지 않는 한 다른 언어로 답하지 않는다.
4. **정형 작업은 다른 에이전트로 분담** — 간단하고 기계적인 작업은 Claude가 혼자 다 하지 않고 다른 AI 에이전트(예: Antigravity)로 라우팅한다. "이건 넘길 만큼 단순한가"를 작업마다 판단. 복잡하거나 판단이 필요한 작업만 메인 에이전트가 직접 처리.
5. **Figma 작업 스코프는 사용자에게 먼저 물어보기** — 파일이 크면 `get_metadata`로 전체 문서를 훑어 화면 목록을 스스로 알아내려 하지 않는다. InnerDerma 작업에서 이렇게 했다가 수십만 자 덤프가 나와 파싱에 여러 번 삽질한 적이 있음. 사용자가 특정 node-id 링크를 줬으면 그 범위만 구현하고, 범위가 불명확하면(링크 없음/여러 화면) **전체 트리를 탐색하지 말고 사용자에게 직접 물어본다.**
6. **Figma 구현은 스크린샷만 보지 말고 전체 컨텍스트를 읽기** — 스크린샷만으로 재구성하면 상태 뱃지 색, 카드 배경, 라벨 텍스트 같은 세부사항이 미묘하게 어긋난다. 스크린샷으로는 안 보이는 것들: 항목별 고정 색 vs UI 선택 상태 색의 구분, 비슷하지만 다른 라벨 텍스트, 카드가 다크/화이트 테마인지 같은 구조적 선택. 상태가 많은 화면일수록 `get_design_context`가 반환하는 실제 JSX/Tailwind 전체를 (필요하면 청크로 나눠서라도) 끝까지 읽고 구현한다.

> **왜 파일에 규칙을 쓰나?** 매번 말로 설명하는 대신 저장소에 적어두면 AI든 새 팀원이든 같은 문서를 읽고 같은 방식으로 일한다. 실제로 겪은 삽질(Figma 전체 덤프, 스크린샷만 보고 놓친 디테일)이 있으면 그걸 규칙으로 남겨 재발을 막는다.

---

## 7. 작업 태도 — 오늘은 속도가 우선

- 백엔드 연동, 에러 핸들링, 엣지케이스 방어 코드는 만들지 않는다 — 오늘 산출물은 "화면과 플로우를 보여주는 것"이 목적.
- 공통 컴포넌트화·상태관리 설계도 최소한만 한다. 다만 2단계에서 참고할 수 있도록 화면 구조와 라우트 구성은 명확하게 남긴다.
- 디자인 토큰(색·폰트)은 그래도 단일 소스 파일 하나에만 정의해서 화면마다 색이 어긋나는 것만은 방지한다 — 캡처물 품질에 직접 영향을 준다.

---

## 8. 문서/규칙 계층 — 하네스 구조 (프로젝트 세팅 시 같이 잡아두면 좋음)

- **루트 `CLAUDE.md`(또는 `AGENTS.md`)** — 이 레포에서 항상 지켜야 할 절대 규칙(위 6번). 배포 경계, 커밋 정책, 언어, 에이전트 분담, Figma 스코프 규칙.
- **디렉토리별 `AGENTS.md`** — 각 디렉토리의 목적·핵심 파일·서브디렉토리·의존관계를 기술한 온보딩 문서. AI 에이전트가 새 세션에서 빠르게 컨텍스트를 잡도록 계층적으로 배치.
- **배포/인프라 문서는 "이 레포가 맡는 부분"과 "전체 인프라 관점"을 분리** — 이 레포가 어디까지 책임지고 어디부터 다른 레포(백엔드/네이티브 빌드/스토어 배포) 소관인지 명확히 문서화.
- **참고용 외부 문서는 부록으로 남기되 혼동되지 않게 표시** — InnerDerma 레포에는 다른 프로젝트(백오피스)의 스택 정리 문서를 "참고용"으로 남겨두되, `AGENTS.md`에 "이 레포의 스택이 아니다"라고 명시해 혼동을 막았다. 이 브리핑 문서 자체도 새 프로젝트에 가져가면 같은 방식으로 — 예를 들어 `docs/reference/` 같은 곳에 "1단계 착수 시점의 브리핑, 실제 구현과 달라질 수 있음"이라고 표시해 부록으로 두면 나중에 헷갈리지 않는다.

> **핵심 발상**: AI에게 매번 맥락을 설명하는 대신, 사람이 지키던 규칙·경계·워크플로를 저장소 문서로 코드화해서 그 안에서 일하게 한다. 짧고 항상 로드되는 계층(CLAUDE.md)과, 필요할 때만 참고하는 상세 계층(AGENTS.md, 배포 가이드)을 나누면 컨텍스트를 낭비하지 않으면서도 규칙을 지킨다.

---

## 9. 2단계(네이티브 전환) 넘어갈 때 참고사항 — 미리 적어두기

- 1단계에서 확정한 화면 목록·라우트 구성·상태 흐름(플로우)을 그대로 네이티브 화면 설계의 기준으로 삼는다.
- 1단계 코드(React 웹)는 재사용이 아니라 "레퍼런스"로 취급 — 네이티브 스택(React Native/Flutter/Swift+Kotlin 등) 선택은 2단계에서 별도로 결정한다.
- 1단계에서 더미로 처리한 API 연동 지점은 주석/문서로 표시해두면 2단계에서 실제 연동 작업이 빨라진다.
- 1단계에서 세운 검증 방식(픽셀 대조 + 시나리오 자동화)의 사고방식은 2단계 네이티브 앱에도 그대로 옮길 가치가 있다 — 타입 체크만으로 못 잡는 시각/동작 회귀를 메우는 게 목적이라는 원칙은 스택이 바뀌어도 동일하다.

---

## 10. 새 프로젝트 세팅 체크리스트

- [ ] `CLAUDE.md`/`AGENTS.md`에 커밋/푸시 정책, 응답 언어, 인프라 경계를 이 프로젝트 기준으로 써서 넣는다.
- [ ] Figma "스코프 먼저 물어보기"·"스크린샷만 보지 말고 전체 컨텍스트 읽기" 두 규칙을 그대로 넣는다.
- [ ] 디자인 토큰(색·폰트)은 Tailwind v4 `@theme` CSS 블록 하나만을 단일 소스로 삼는다 — 별도 JS 토큰 파일을 만들어 값을 중복 정의하지 않는다.
- [ ] API 호출을 도메인별 모듈로 분리하는 폴더 구조(`src/api/`)를 미리 잡아둔다(오늘은 비어있어도 됨).
- [ ] 단일 뷰포트(iOS 393×852)를 캡처하는 Playwright 스크립트를 하나 만들어둔다.
- [ ] 패키지 설치 시 `react-router-dom`이 아니라 `react-router`(v8)로 설치한다 — 이름이 바뀌었다.
- [ ] TypeScript를 쓰더라도 타입 체크는 레이아웃 좌표·색상·시각적 어긋남까지는 못 잡는다는 전제로, 그 자리를 메울 검증 게이트(픽셀 대조, E2E 시나리오 등)를 처음부터 설계한다.
- [ ] 이 브리핑 문서(또는 InnerDerma 작업방식 정리 문서)를 `docs/reference/` 같은 곳에 부록으로 남기고, "이 프로젝트의 최신 상태가 아니라 착수 시점 참고 자료"라고 표시해 혼동을 막는다.
- [ ] 스택 표(위 2번)에 라이브러리를 추가/교체할 때마다 "왜 이걸 썼는가"를 한 줄씩 남긴다 — 나중에 팀원이 스택을 이해하기 쉬워진다.

---

*InnerDerma 프론트엔드(`innerderma_front`)에서 실제 사용한 구성·규칙을 MALLO 프로젝트(TypeScript, 1단계는 iOS 단일 뷰포트 / 최종 목표는 iOS·Android 대응)용으로 재구성한 브리핑 문서.*
