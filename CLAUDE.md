# MALLO 웹 프로토타입 — 저장소 운영 규칙

이 문서는 이 레포에서 AI 에이전트(Claude Code 등)가 항상 지켜야 할 절대 규칙이다.
배경/기획 맥락은 `MALLO_사업계획서.md`, 이 1단계 작업 방식의 상세 근거는
`MALLO-화면작업-브리핑.md`를 참고한다.

## 0. 지금 이 레포가 하는 일

- **1단계(현재)**: 실제 네이티브 앱이 아니라, Figma 디자인을 iOS 393×852
  단일 뷰포트 기준 웹 프로토타입으로 옮겨 화면·플로우를 캡처·검증하는 것이
  목표. 백엔드 연동 없음, 더미 데이터만 사용.
- **2단계(별도 작업, 이후)**: 이 레포의 화면 구성·라우트·상태 흐름을
  참고 자료로 삼아 실제 네이티브(또는 크로스플랫폼) 앱으로 이식. 이 레포의
  React 코드 자체는 재사용 대상이 아니라 레퍼런스.
- Figma 소스: `https://www.figma.com/design/KGjqGDv3jZO3WxZvDs278x/MALLO`.
  구현 대상 화면 16개와 각 화면의 node-id는 `src/routes/screens.ts`에
  단일 소스로 정리되어 있다.

## 1. 배포/인프라 경계

이 레포는 **프론트엔드 화면/플로우 코드까지만** 책임진다. 백엔드 API, 인증
서버, 스토어 배포 파이프라인, 도메인/인증서 등은 이 레포의 범위 밖이다.
경계 밖 변경이 필요해 보이면 실행 전 사용자에게 먼저 확인한다.

## 2. 커밋/푸시 정책

기본 원칙은 **검증(빌드·타입체크·주요 동작 확인) 후에만** 커밋한다.
이 프로젝트에서는 사용자가 "작업하면서 중간중간에 커밋과 푸시도 진행해달라"고
명시적으로 요청했으므로, 의미 있는 체크포인트(예: 화면 하나 완성, 스캐폴딩
완료)마다 매번 별도 승인을 구하지 않고 커밋 후 `origin main`으로 푸시한다.
다만 `--force`, 히스토리 재작성, 브랜치 삭제 등 되돌리기 어려운 작업은
여전히 사용자 확인 없이 하지 않는다.

## 3. 응답 언어

모든 응답은 한글로 한다. 사용자가 다른 언어를 명시하지 않는 한 다른 언어로
답하지 않는다.

## 4. 정형 작업 분담

간단하고 기계적인 작업(반복적인 화면 이식, 더미 데이터 채우기 등)은 상황에
따라 다른 에이전트로 라우팅할 수 있다. 복잡하거나 판단이 필요한 작업만
메인 에이전트가 직접 처리한다.

## 5. Figma 작업 스코프

파일이 크면 `get_metadata`로 전체 문서를 훑어 화면 목록을 스스로 알아내려
하지 않는다. 사용자가 특정 node-id를 줬으면 그 범위만 구현한다. 범위가
불명확하면(링크 없음/여러 화면/그룹 간 관계 불명확) 전체 트리를 탐색하지
말고 사용자에게 먼저 물어본다. 이번 세션에서는 이미 화면 16개와 node-id
매핑을 사용자와 함께 확정했다 — `src/routes/screens.ts` 참고.

## 6. Figma 구현 시 전체 컨텍스트 읽기

스크린샷만 보고 재구성하지 않는다. 상태 뱃지 색, 카드 배경, 라벨 텍스트,
다크/화이트 테마 여부 같은 세부사항은 스크린샷으로 안 보인다.
`get_design_context`가 반환하는 JSX/Tailwind 전체를 끝까지 읽고 프로젝트
컨벤션(아래 7번)에 맞게 변환한다.

## 7. 아키텍처 규칙

- **디자인 토큰 단일 소스**: `src/index.css`의 `@theme` 블록 하나만이
  색·폰트 토큰의 소스다. 컴포넌트에서 색상 리터럴(`#fff`, `text-[#...]`)을
  직접 쓰지 않는다. Figma에서 새 색이 나오면 `@theme`에 이름을 붙여 추가한다.
- **API는 도메인 모듈 경유만**: `src/api/<도메인>.ts`를 거친다(1단계는
  비어 있어도 됨). 화면 컴포넌트가 HTTP 클라이언트를 직접 호출하지 않는다.
- **오버레이는 라우트가 아니다**: 모달·바텀시트는 URL을 바꾸지 않고
  `src/store/uiStore.ts`의 전역 상태로 연다/닫는다.
- **모바일 프레임은 스케일로만 축소**: `DeviceFrame`의 고정 크기(393×852)를
  유지하고 내부 좌표를 줄이지 않는다.
- **고정 레이어와 스크롤 영역 분리**: 헤더/탭바는 스크롤 밖에 고정, 본문만
  `ScrollArea`로 스크롤.
- **날짜/시간은 항상 실계산**: 특정 날짜를 상수로 박지 않는다. 검증
  스크립트의 기대값도 실행 시점 기준으로 계산한다.
- **정적 디자인과 실제 동작이 어긋나면 동작을 택한다**: Figma는 스냅샷
  한 장이라 빈 값/부분 선택/비활성 같은 상태를 다 표현하지 못한다.
- **디자인 출력은 그대로 믿지 않는다**: 숨겨진 레이어, 빈 프레임, 겹친
  레이어 등으로 실제 렌더가 다를 수 있다. 스크린샷·픽셀 실측으로 재확인.
- **이미지/아이콘은 직접 그리지 않는다**: `get_design_context`가 반환하는
  asset URL을 다운로드해 `src/assets/`에 커밋한다(URL은 ~7일 후 만료).

## 8. 검증

- 픽셀 대조: 구현 화면 캡처 vs Figma 원본을 좌/중/우 3분할로 비교.
- 기능 자동 검증: Playwright(`e2e/`)로 스크롤·모달·폼·상태동기화·날짜이동
  등 시나리오 검증. 로케일은 `ko-KR` 고정(`playwright.config.ts`에 설정됨).
- 카메라 등 `getUserMedia`류 기능이 들어가면, LAN IP로 다른 기기 접속 시
  HTTPS·자체서명 인증서 허용 옵션이 필요할 수 있다.

## 9. 오늘(1단계) 작업 태도

백엔드 연동, 에러 핸들링, 엣지케이스 방어 코드는 만들지 않는다. 공통
컴포넌트화·상태관리 설계도 최소한만 한다. 디자인 토큰만은 단일 소스로
유지해 화면마다 색이 어긋나는 것을 방지한다.
